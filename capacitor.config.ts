import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.samp.nexus',
  appName: 'SAMP Nexus',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
